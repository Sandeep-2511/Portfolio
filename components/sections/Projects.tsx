import type { FC } from "react";

/**
 * Projects section groups featured work and case studies.
 * Leave this component content-free until the UI composition stage.
 */
const Projects: FC = () => {
  return null;
};

export default Projects;
