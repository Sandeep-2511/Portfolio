import type { FC } from "react";

/**
 * About section contains developer background and positioning content.
 * Keep this data-driven and reusable across the portfolio.
 */
const About: FC = () => {
  return null;
};

export default About;
