import type { FC } from "react";

/**
 * Hero section represents the top-level introduction and key message.
 * Keep this section independent so it can be composed into the page layout.
 */
const Hero: FC = () => {
  return null;
};

export default Hero;
