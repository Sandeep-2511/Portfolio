import type { FC } from "react";

/**
 * Contact section exposes communication channels and calls to action.
 * Keep this isolated from layout to preserve reuse across pages.
 */
const Contact: FC = () => {
  return null;
};

export default Contact;
