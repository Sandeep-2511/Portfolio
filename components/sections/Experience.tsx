import type { FC } from "react";

/**
 * Experience section displays professional background and role history.
 * Separate this from page layout so it can be reused or reordered.
 */
const Experience: FC = () => {
  return null;
};

export default Experience;
