import type { FC } from "react";

/**
 * Footer contains secondary links, copyright, and global site notes.
 * Separate shell layout from section content for reusable structure.
 */
const Footer: FC = () => {
  return null;
};

export default Footer;
