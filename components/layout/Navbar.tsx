import type { FC } from "react";

/**
 * Navbar contains global navigation and brand structure.
 * Keep this focused on layout composition and reusable logic.
 */
const Navbar: FC = () => {
  return null;
};

export default Navbar;
